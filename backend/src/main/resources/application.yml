spring:
  application:
    name: blog-backend
  profiles:
    active: dev

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.example.blog: DEBUG

security:
  sensitive-words:
    - 赌博
    - 诈骗
    - 色情
    - 违禁
